# Docker build context allowlist. The base image needs only its build recipe,
# dispatcher, metadata parser, and declarative manifest; tests and local state
# must never be transferred into the image build context.
*
!Dockerfile
!image.manifest
!runner
!runner-metadata.sh
